# Graph-JEPA

Adaptation of [I-JEPA](https://arxiv.org/abs/2301.08243) method to graph domain.

## Installation
1. CPU environment
    ```shell
    make install_cpu
    ```
2. GPU environment
    ```shell
    make install_gpu
    ```
