# @package _global_

defaults:
  - override /training: training
  - override /dataset: amazon_ratings
  - override /pos_encoding: null
  - override /model: ???
  - override /backbone@model.backbone: hetero_sage_3_layer
