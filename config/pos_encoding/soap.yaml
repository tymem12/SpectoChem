name: soap
file: data/pos_encoding/${dataset.name}/${.name}.pt
dim: 220
transforms:
  SOAPPE:
    attr_name: positional_encoding
    l_max: 3
    num_radial: 10
    r_max: 5.0
    use_cutoff: true
    center: false
    use_existing_edges: false
    include_input: false
  # SOAP_Dsribe:
  #   species: [1, 6, 7, 8, 16] # Only organic
  #   rcut: 5.0
  #   nmax: 6 #10
  #   lmax: 3
  #   sigma: 0.6 #0.5
  #   sparse: true
  #   attr_name: positional_encoding


